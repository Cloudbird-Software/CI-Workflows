# test-integrity.yml 依赖（ADR-0035）：policy（testing.yaml）解析用 PyYAML。
# 版本+sha256 双锚定，与 .github/.github/requirements-gate.txt / requirements-diff-coverage.txt
# 同款钉法；哈希对应 cp312 manylinux x86_64 wheel（job 固定 python 3.12 + ubuntu-latest），
# 升级须同步换哈希。
pyyaml==6.0.3 --hash=sha256:ba1cc08a7ccde2d2ec775841541641e4548226580ab850948cbfda66a1befcdc
